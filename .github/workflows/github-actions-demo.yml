name: Swift

on:
  [push]
    

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@radhika/add-tests
    - name: Build
      working-directory: ./MyLibrary
      run: swift build -v
    - name: Run tests
      working-directory: ./MyLibrary
      run: swift test -v

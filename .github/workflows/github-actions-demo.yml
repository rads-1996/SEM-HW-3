name: Swift

on: [push]

jobs:
  build:

    runs-on: macos-latest

    steps:
      - uses: actions/checkout@radhika/add-tests
      - name: Build
        run: swift build
      - name: Run tests
        run: swift test
